{
  "name": "polyhymnia-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "typecheck": "pnpm -r --filter=./packages/* typecheck",
    "test": "pnpm -r --filter=./packages/* test",
    "dev": "pnpm --filter @polyhymnia/web dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
